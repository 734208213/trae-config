# ============================================================================
# Trae 全局配置目录 .gitignore
# 说明：标记不需要同步到 GitHub 的自动生成/可重新下载的文件
# ============================================================================
# 同步原则：
#   KEEP   → 用户自主创建、不可重新生成的内容（规则、配置偏好、项目经验）
#   IGNORE → 随 Trae 安装自带、可从市场重新下载、会话自动生成的内容
# ============================================================================


# ---------------------------
# argv.json - 机器级配置
#   crash-reporter-id 是机器唯一标识，不同机器不同
#   拉取后 Trae 会自动用当前机器的 ID 重新生成
# ---------------------------
argv.json


# ---------------------------
# builtin_skills/ - Trae 内置技能
#   随 Trae IDE 安装包一起发布，重新安装 Trae 即可恢复
# ---------------------------
builtin_skills/


# ---------------------------
# builtin/ - Trae 内置技能库
#   按模型角色（default/deidamia/medea/penelope/eurydice/thetis/hebe）
#   预装 skills（code/trae/work），随 Trae IDE 安装包发布
#   包含 ide_version.json、version.json
#   重新安装 Trae 即可恢复
# ---------------------------
builtin/

# ---------------------------
# extensions/ - 扩展插件本体
#   从 VS Code Marketplace 下载，按扩展 ID 重新安装
#   但保留 extensions.json（记录了所有已安装扩展的 ID 和版本），
#   供 Trae 检测后自动恢复
#   恢复方式：
#     方式一（自动）：Trae 检测到 extensions.json 后自动恢复
#     方式二（手动）：`code --install-extension <publisher.extension>`
# ---------------------------
extensions/*
!extensions/extensions.json


# ---------------------------
# mcps/ - MCP 服务器配置
#   按工作区/会话自动生成，目录名含会话哈希（s_HID-* / s_wx_*）
#   拉取后 Trae 启动时会自动重新创建
# ---------------------------
mcps/

# ---------------------------
# memory/ - 记忆系统
#   保留：user_profile.md（用户画像）、project_memory.md（项目经验）
#   忽略：会话明细、主题摘要、清理标记（自动生成）
# ---------------------------
memory/.cleanup/
memory/**/session_memory_*.jsonl
memory/**/topics.md


# ---------------------------
# worktrees/ - 工作树
#   空目录，Trae 按需自动创建
# ---------------------------
worktrees/